# 视频链接
[FPS游戏开发教程 | UE5制作第一人称射击游戏 | 虚幻引擎5.7 零基础入门](https://www.bilibili.com/video/BV1aw9jBeEHr?p=4&vd_source=5d0525b6127592d0599bf5f5308fa0e6)


## 一、配置游戏模式和输入：玩家按键配置
游戏模式：蓝图类 -> 游戏模式基础

输入：输入 -> 输入操作和输入映射情景（Move、Look、Run、Aim）

![alt text](assets/image-1.png)

注意事项：想象二维坐标系，向右和向上是x和y轴的正向，针对游戏中就是角色的向前和向右向量，所有A和S进行向后和向左移动时，使用否定修改器。此外W、S和A、D使用不同的轴，所以在A、D上添加拌合输入轴值来对应不同的轴。


## 二、实现基础移动能力（视角旋转、前后左右移动）

实现鼠标控制视角旋转和WSAD控制角色水平移动。

### 1 绑定输入映射上下文(IMC 也翻译为输入映射情景)

![alt text](assets/image-2.png)

对于多人游戏来说，应该先获取自身控制器，然后将其转换为玩家控制器来绑定IMC。

### 2 实现视角旋转

虚幻中鼠标上滑默认是视角向下旋转的。处理方式是在IMC中在设置IA_Look时，添加一个否定修改器，来讲Y轴取反。

![alt text](assets/image-4.png)

Pitch：绕X轴旋转，即鼠标上下滑动实现视角的抬头和低头。

Yaw：绕Z轴旋转，即鼠标左右滑动实现视角的左右水平旋转。

Roll：绕X轴旋转，如翻跟头。

![alt text](assets/image-3.png)

### 3 实现基础移动

玩家的移动应该是按视角的旋转来旋转移动方向，所以应该从控制器旋转获取其向前、向右向量。

我们要实现玩家水平移动，应该只获取控制器在XY面上的方向，即只获取其XY平面（Yaw）的方向，如果把Pitch加入，玩家的移动会随着抬头减慢移动，最终随着角度增大会停止移动。

Q1：如果获取XY平面的方向，玩家抬头后，方向向量是(0,0,1)，获取Yaw也是(0,0)，按理说应该也是不能移动。

A1:如果我们抬起头看天，随着旋转，其实下巴指向的方向也是会变的，类似这种。 

![alt text](assets/image-5.png)


## 三、完善角色细节（添加相机、手臂、枪）
完成手臂、枪（基础枪身、枪管、弹夹、枪托等骨骼绑定）

![alt text](assets/image-7.png)

### 1 枪和手臂贴合
目前使用动画资产，实现简单的效果，直接设置手臂和墙身对应的动画资产即可。

![alt text](assets/image-8.png)

### 2 枪管、弹夹、枪托与墙身绑定。

在插槽处选择对应的插槽即可。

![alt text](assets/image-9.png)

## 四、角色行动能力系统（run、walk、aim状态变量设置）

### 1 角色状态枚举

创建一个枚举蓝图类，设置角色的四个行为（idle、walk、run、aim）

![alt text](assets/image-10.png)

### 2 角色状态变量设置

![alt text](assets/image-12.png)

- Move XY 是角色的前后左右移动

- Want to Aim Run 是角色的瞄准和跑

- E_PlayerState 是角色的当前状态枚举值

### 3 角色状态变量更新逻辑(函数)

对于run和aim，在按相应按键后，设置一个变量保存

- Move XY
    此时X>0时可用于判断是否向前移动，目前Y没有用  
    ![alt text](assets/image-13.png)

- Want to Aim Run 是角色的瞄准和跑
    无需多言
    ![alt text](assets/image-11.png)

- E_PlayerState 是角色的当前状态枚举值
    根据前面的变量和角色移动组件的移动速度来设置，目前设置的状态优先级：瞄准aim > 跑步run > 行走walk > 空闲idle
    ![alt text](assets/image-14.png)

- update move行走速度更新函数
    根据E_PlayerState设置玩家的最大行走速度，实现移动速度随状态变化
    ![alt text](assets/image-15.png)

### 4 事件Tick中更新状态

![alt text](assets/image-16.png)

## 实现效果

左上角会显示速度和状态  
<video controls src="assets/stg_2026-05-06_00-02-26.mp4" title="Title"></video>




## 五、动画系统（walk、idle、run、aim等持枪动画与转换）

本节实现手臂动画和枪的动画一致性，并且支持动画在idle、walk、run、aim丝滑切换，不卡手。由于角色的动画和枪的动画实现逻辑一致，后续讲解不涉及枪了。

### 1 相关资产介绍

代码存放目录

![alt text](assets/image-17.png)

使用动画蓝图和混合空间进行动画系统开发：

![alt text](assets/image-18.png)

- 动画蓝图：使用状态机来控制动画切换逻辑
![alt text](assets/image-19.png)
- 混合空间：可以使用二维坐标系，来将不同的动画进行混合播放
![alt text](assets/image-20.png)


### 2 实现流程概述
1. 对于稍微复杂的，创建BS混合空间处理Move(Idle、前后左右Walk)，可以适当参考我们创建的IA输入操作，每个操作基本上就是一个单独的动画，而且还需要考虑操作直接的过度动画。
![alt text](assets/image-24.png)
2. 创建动画蓝图，实现不同状态的动画以及状态切换。
3. 将动画蓝图实装到角色蓝图中。

### 3 混合空间 Move实现
使用混合空间，设置每个轴代表的含义：
- X轴（水平坐标）代表左右移动动画
- Y轴（垂直坐标）代表前后移动动画
- 网格分化为2即可，一共三个点，中间的点代表idle站立即可。

![alt text](assets/image-21.png)

知识回顾：在实现角色移动时，我们使用了一个IA_Move是一个二维向量(X,Y)，每个值可以为-1,0,1。其中对于X轴，设置为W/S进行前后移动，对于Y轴，设置A/D进行左右移动。
那么为什么不是X代表左右、Y代表前后呢？

![alt text](assets/image-22.png)

打开角色蓝图，可以发现，X轴正方向就是角色的向前方向。结合引擎提供获取角色的向前和向右向量，可以得知UE引擎底层设计就是向前和向右分别是X，Y轴的正方向，且默认X的右手是Y轴正方向。可能是这种符合人类直觉，因为在将WS/AD作为角色前后/左右移动时，确实X/Y轴可以直接对应。但正常人逻辑（或者说最常见的XY坐标系是X正方方是向右、Y的正方向是向前）可能会角色反直觉，正常人应该想的是俯视看角色，角色的坐标系应该为X左右，Y上下的。

服了，豆包绝了，问个问题给我颠倒黑白的。

![alt text](assets/7a133c955d9ee4fd873a9eb0f6a25416.png)

将不同的资产动画拖入，X轴是左右，Y轴是前后，记得中间别忘了Idle动画。可以注意这里我们X轴是左右，和设计角色移动时的X是前后是相反的。这里原则上也可以设置为X为前后，Y为左右，但现在这种设计比较符合直觉（通常坐标系是X是左右）。

![alt text](assets/image-23.png)

目前实现了Move的动画设置。

### 4 动画蓝图实现动画切换

获取实装到的角色蓝图，将需要的变量获取，可提前获取，也可在编写状态机时按需获取。

![alt text](assets/image-27.png)

创建一个basic状态机

![alt text](assets/image-25.png)

设计不同状态的切换逻辑，利用角色蓝图中保存的相关表示状态的变量实现不同状态之间的跳转。

![alt text](assets/image-26.png)

- 对于Move，将Move Y作为X轴（左右）变量，Move X作为Y轴（前后）变量
![alt text](assets/image-28.png)
- 对于Aim，需要考虑是站立瞄准还是walk时瞄准
![alt text](assets/image-29.png)
- 对于ToAim和ToIdle需要有相关动画资产才可以实现，且该状态是为了让操作丝滑跟手。如果不设置，用户快速点击aim瞄准键，会出现，枪前瞄准，等瞄准动画完成后再做瞄准动作，会有一种停顿感。

### 角色蓝图中实装效果
角色和武器的动画蓝图实现逻辑相同，且要保持里面使用到的一些关键参数需要保存一致（判断动画剩余时间>x时，x在两者中保持一致）。

<video controls src="assets/stg_2026-05-12_22-19-12.mp4" title="Title"></video>

## 六、模块化设计+Bug

### 1 强耦合关系

角色与武器强耦合

![alt text](assets/image-30.png)

### 2 解耦合

创建一个强的基类（有通用变量，如子弹数，弹夹数等），在创建一个卡宾枪的类基础该基类。

![alt text](assets/image-31.png)

在角色蓝图中，创建一个函数，用来加强的蓝图类绑定为自己的组件，然后在角色蓝图的时间开始运行后执行该函数。

![alt text](assets/image-32.png)

### 3 模块化设计前、设计后都存在的bug

cast to 是比较费时和费内存的操作，为了提升性能，将其在动画蓝图初始化时就将其获取，保存为自身的变量，就不需要反复cast to了。

![alt text](assets/image-34.png)

我们保存的是玩家角色的对象引用，当动画更新时时手臂和枪是同时向玩家角色对象获取状态信息的，此时就可能出现他们获取的时间不是同一帧。

处理思路：可以让武器直接获取手臂的动画蓝图对象，使用其保存的状态信息，可以保证武器的动画始终是在手臂之后执行的，与手臂对其。

![alt text](assets/image-33.png)

bug演示：run时疯狂aim

<video controls src="assets/stg_2026-05-13_00-55-12.mp4" title="Title"></video>

修改后：

<video controls src="assets/stg_2026-05-13_00-55-12-1.mp4" title="Title"></video>