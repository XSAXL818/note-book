# 2026-08-25

## 课件目录不推送 GitHub
- 需求：`src/games101/课件/`（23 个 PDF）不推送到 GitHub。
- 完成的操作：
  1. `.gitignore` 追加 `/src/games101/课件/`（带注释"体积大不推送，仅本地使用"）。
  2. 课件 PDF 此前**已被 git 跟踪**，仅加 .gitignore 不够，执行 `git rm -r --cached "src/games101/*/GAMES101_Lecture_*.pdf"` 将 23 个 PDF 移出索引（`--cached` 保留本地文件）。
- 中文路径坑（再次确认）：PowerShell 传中文路径给 git 会乱码（"课件"→"璇句欢"）导致 pathspec 匹配失败，需用 ASCII 通配符（如 `*/GAMES101_Lecture_*.pdf`）绕过。
- 待办：用户需 `git add .gitignore` + commit + push 才彻底生效；历史提交里已存在的 PDF 不会被自动清除（如需清除历史可用 filter-repo，一般没必要）。
- 本地 mdbook build 不受影响（gitignore 不影响文件系统），课件链接仍可用。
<<<<<<< HEAD

## 课件链接改为本地绝对路径映射
- 用户把课件移到 `C:\Users\75271\Desktop\GameProject\res\Games101课件\`（23 个 PDF 齐全）。
- `src/图形学入门.md` 中全部课件链接由相对路径 `./games101/课件/GAMES101_Lecture_XX.pdf` 改为 `file:///C:/Users/75271/Desktop/GameProject/res/Games101课件/GAMES101_Lecture_XX.pdf`（replace_all 一次替换）。
- 验证：mdbook build 通过，输出 HTML 中 `<a href="file:///C:/Users/...">` 原样保留，中文路径无编码问题。
- 注意：`src/games101/课件/` 本地旧副本仍保留（mdbook 会继续复制到 book/，但链接已不指向它）；若不再需要可自行删除。这是绝对路径映射，换电脑后需同步修改新位置。
- 已按用户确认执行 `Remove-Item -Recurse -Force "src/games101/课件"` 删除旧副本，释放约 500MB；因之前已 `git rm --cached`，删除磁盘文件不影响 git 状态。旧链接均已指向新位置，mdbook 构建不受影响（book 输出里之前复制的旧 PDF 随下次 clean build 或手动清理去除）。

## file:// 链接在 http 页面点击无效 → 改用 junction（最终方案）
- **关键知识点**：mdbook serve 页面是 `http://localhost`，浏览器禁止从 http 页面跳转到 `file:///` 本地文件（Chrome/Edge 静默拦截），所以"本地绝对路径映射"方案点击没反应。
- **最终方案**：`src/games101/课件` 创建 junction（`cmd /c mklink /J "src\games101\课件" "C:\Users\75271\Desktop\GameProject\res\Games101课件"`），课件实体留在 GameProject（磁盘零复制），mdbook build 会把 junction 内容正常复制到 book/ 输出，链接用相对路径 `./games101/课件/xxx.pdf`，通过 http 正常打开。
- 验证：`mdbook clean && mdbook build` 后 book/games101/课件/ 有全部 23 个 PDF；HTML 链接为相对路径；git 不跟踪 junction（已 gitignore）。
- 注意：PowerShell 临时脚本中文路径会乱码（假阴性），验证中文路径用 ASCII 通配符（`book/games101/*` + `-Filter "GAMES101_Lecture_*.pdf"`）。
=======
>>>>>>> 3aaf375bea32c382896146dec45c83219918cfe7
