# 2026-09-01

## GAMES101 课件映射迁移（junction 重建）
- 用户把课件从 `C:\Users\75271\Desktop\GameProject\res\Games101课件`（旧，已删）移到 `C:\Users\75271\Desktop\GameProject\Games101\课件`（新，23 个 PDF）。
- 重建 junction：`cmd /c mklink /J src\games101\课件 <新路径>`，成功；`src/games101` 下原 junction 已不存在（用户移走课件后失效）。
- `.gitignore` 里之前的课件忽略规则丢失，已用 replace_in_file 重新追加 `/src/games101/课件/`（用工具写入避免 PowerShell 中文乱码）；`git status --porcelain --ignored=matching` 确认该目录显示 `!!`（已忽略，不会上传）。
- `mdbook build` 通过，`book/games101/课件/` 含 23 个 PDF。
- 表格链接现状（最终方案）：`src/图形学入门.md` 中 23 处 `Lecture_xx` 为**纯文本**（无链接），含 Lecture_04_supp，build 通过。
- **结论（用户最终选择）**：file:// 链接在 mdbook serve（http://localhost）下点击被浏览器静默拦截 → 用户放弃链接，只要纯文本 Lecture_xx。相对路径+junction 方案虽在 serve 下可点，但用户不想要课件进项目/复制进 book，已弃用；junction 已删除。
- **坑（重要）**：mdbook serve 的 http 页面点击 file:// 链接必被浏览器拦截；file:// 链接需双击 book/index.html 打开页面才可跳转。
- junction `src/games101/课件` 已删除（用户确认"删掉"）；`mdbook clean` 清掉 book/ 里 319MB 课件副本；课件实体 23 个 PDF 完好。`.gitignore` 里 `/src/games101/课件/` 规则保留（无害）。

## 待用户决定
- 用户问过 `.codebuddy/memory/*.md`（含 2026-08-14/24/25）已被 git 跟踪、push 会公开上传，未决定是否 `git rm -r --cached .codebuddy` 排除。
- `theme/css/custom.css`（遗留死代码，未被 book.toml 引用）删除请求曾超时未确认，用户未表态；`src/assets/games101-syllabus.css` 为同内容副本。

## work.md 知识大纲
- `src/games101/work.md`（原为空）已整理为 GAMES101 笔记知识大纲：1 概述（四大主题/光栅化/曲线网格/光线追踪/动画）、2 向量与线性代数（向量基础/点积/叉积，均取自 work1/work2 实内容）+ 子笔记相对链接（./work1.md 等，mdbook 内可跳转）；3~7 标注占位待整理。build 通过。
