# 2026-08-14

## PowerShell 配置修改
- 在 `c:\Users\75271\Documents\PowerShell\Microsoft.PowerShell_profile.ps1` 中新增了 `notebook` 函数，调用 `& "C:\Users\75271\Desktop\1打开笔记.ps1"`。
- `1打开笔记.ps1` 的作用：用 VS Code 打开 `C:\Users\75271\Desktop\Project\MdBook\note-book`，启动 `mdbook serve` 并打开浏览器预览 http://localhost:3000。
- 用户备注：输入 `notebook` 即可一键打开笔记项目。
- 新增快捷方式管理：profile 中加入 `$script:ShortcutHelp` 有序哈希表（命令名→说明）统一登记快捷方式，`Show-Shortcuts` 函数打印全部（支持 `-Name` 过滤），别名 `mycmds`。避免使用 `sc` 别名（与内置 Set-Content 冲突）。
- mdBook 笔记：SUMMARY.md 中 `- [笔记](./03-ue/sub/06-cpp-笔记.md)` 改为 `- 笔记`（无链接分组标题），实现"可点击展开子项但不打开页面"。book.toml 已启用 `[output.html.fold] enable=true level=2`。
